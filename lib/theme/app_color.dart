import 'package:flutter/material.dart';

class AppColor {
  static const primary = Color(0xFF6FCF97); // hijau lembut
  static const secondary = Color(0xFF56CCF2); // biru muda
  static const accent = Color(0xFFF2994A); // orange lembut
  static const background = Colors.white;
}
